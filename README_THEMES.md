# 🎮 AESCULAPIUS - Система переключения тем

## ⚡ Быстрый старт

### Что было сделано?

✅ **Создана полная система переключения интерфейсных тем** между двумя режимами:

1. **Default Mode (Зелёный CRT)** - Классический стиль
2. **Fallout Mode (Янтарный Pip-Boy)** - Атомпанк эстетика

### Основные файлы:

```
📂 static/
  📄 fallout-theme.css      ← Новый файл (595 строк CSS)
  📄 login_style.css        ← Обновлено
  📄 retro_style.css        ← Обновлено

📂 templates/
  📄 login.html             ← Обновлено (кнопка + логика)
  📄 terminal.html          ← Обновлено (скрытая кнопка + логика)

📄 THEME_SYSTEM.md          ← Техническая документация
📄 QUICK_START_THEME.md     ← Пользовательское руководство
📄 IMPLEMENTATION_REPORT.md ← Отчет о реализации
📄 FINAL_CHECKLIST.md       ← Финальный чеклист
```

---

## 🎯 Как использовать

### 1️⃣ На странице логина:

1. Найдите кнопку **▬** (справа сверху)
2. Нажмите кнопку
3. Интерфейс переключится на **Fallout-режим** (янтарный монохром)
4. Нажмите снова, чтобы вернуться в **стандартный режим** (зелёный CRT)

### 2️⃣ На главной странице:

1. Наведите мышку на **верхний правый угол** панели управления
2. Появится скрытая кнопка переключения
3. Нажмите кнопку для переключения темы

### 3️⃣ Сохранение выбора:

- Ваш выбор **автоматически сохраняется**
- При перезагрузке страницы тема **остаётся прежней**
- При логауте и новом входе тема **сохраняется**

---

## 🎨 Визуальные характеристики

| Параметр | Default | Fallout |
|----------|---------|---------|
| **Основной цвет** | `#33ff33` 🟢 | `#FFB642` 🟡 |
| **Фон** | `#020202` | `#050505` |
| **Эффект** | Зелёный монохром | Янтарный монохром |
| **Эстетика** | Классический CRT | Fallout Pip-Boy |
| **Сканлайны** | Зелёные | Янтарные |
| **Время переходов** | - | 0.6s |

---

## 🔧 Для разработчиков

### Принцип работы:

```
User clicks button
    ↓
JavaScript toggles class "theme-fallout"
    ↓
CSS transitions activate
    ↓
localStorage saves state
    ↓
Page reloads with saved theme
```

### Сохранение в localStorage:

```javascript
localStorage.setItem('themeMode', 'fallout'); // или 'default'
```

### Проверить текущую тему:

```javascript
console.log(localStorage.getItem('themeMode')); // 'fallout' или null
console.log(document.body.classList.contains('theme-fallout')); // true/false
```

### CSS-переменные Fallout-режима:

```css
--pipboy-primary: #FFB642;      /* Янтарный */
--pipboy-primary-dark: #CC8A2F; /* Тёмный янтарь */
--pipboy-accent: #FFD700;       /* Яркий жёлтый */
--pipboy-danger: #FF6B35;       /* Оранжевый */
--pipboy-bg: #0F0F0F;           /* Чёрный фон */
```

---

## ✨ Ключевые особенности

### ✅ Консервирование

- Все исходные стили **полностью сохранены**
- Ничего не удалено, только добавлено
- Работает как **наслоение**, а не замена

### ✅ Производительность

- CSS transitions вместо JavaScript анимаций
- Минимальные переговоры браузера
- Плавные переходы без заиканий

### ✅ Кросс-браузерная совместимость

- Chrome/Edge ✓
- Firefox ✓
- Safari ✓
- Все современные браузеры ✓

### ✅ Архитектура

- Модульная структура
- Легко добавить новую тему
- Чистый и понятный код
- Полная документация

---

## 📊 Статистика

| Метрика | Значение |
|---------|----------|
| Новых файлов CSS | 1 |
| Добавлено строк CSS | ~1100 |
| Обновленных HTML файлов | 2 |
| Добавлено строк JavaScript | ~80 |
| Документации файлов | 4 |
| Консервировано кода | 100% |
| CSS-переменные | 7 |
| Анимации | 8+ |

---

## 🎓 Примеры использования

### Переключить тему из консоли:

```javascript
// Включить Fallout-режим
function enableFallout() {
    document.body.classList.add('theme-fallout');
    document.documentElement.classList.add('theme-fallout');
    localStorage.setItem('themeMode', 'fallout');
}

// Выключить Fallout-режим
function disableFallout() {
    document.body.classList.remove('theme-fallout');
    document.documentElement.classList.remove('theme-fallout');
    localStorage.setItem('themeMode', 'default');
}

// Переключить (toggle)
function toggleTheme() {
    if (localStorage.getItem('themeMode') === 'fallout') {
        disableFallout();
    } else {
        enableFallout();
    }
}
```

### Проверить текущую тему:

```javascript
function getCurrentTheme() {
    return localStorage.getItem('themeMode') || 'default';
}

console.log(getCurrentTheme()); // 'fallout' или 'default'
```

---





**Версия:** 1.0  
**Дата:** 2026-02-02  

