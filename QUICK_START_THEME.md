# Краткое руководство: Переключение тем AESCULAPIUS

## 🎮 Как пользователю переключить тему

### На странице логина:
1. Найдите кнопку с символом **▬** рядом с кнопкой INFO (справа сверху)
2. Нажмите кнопку - интерфейс перейдёт в режим **Fallout: New Vegas** (янтарный монохром)
3. При нажатии снова вернётесь на стандартный режим (зелёный CRT)

### На главной странице:
1. Наведите мышку на **верхний правый угол** панели управления (control-panel)
2. Появится скрытая кнопка переключения тем
3. Нажмите кнопку для переключения

### Сохранение выбора:
- Ваш выбор автоматически сохраняется
- При обновлении страницы тема останется той же
- При логауте и новом логине тема также сохранится

---

## 🛠️ Для разработчиков

### Структура решения

**Новые файлы:**
- `static/fallout-theme.css` - Полный стиль Fallout-режима (~1000 строк CSS)

**Изменённые файлы:**
- `templates/login.html` - Добавлена кнопка переключения + JavaScript логика
- `templates/terminal.html` - Добавлена скрытая кнопка + JavaScript логика
- `static/login_style.css` - Добавлены стили для кнопки переключения
- `static/retro_style.css` - Добавлены стили для скрытой кнопки терминала

### Как добавить новую тему

1. Создайте новый файл `static/new-theme.css`
2. Используйте селектор `html.theme-new-name` или `body.theme-new-name` для изоляции
3. Переопределите переменные и стили
4. Подключите файл в `<head>` обоих HTML-файлов
5. Обновите JavaScript-логику для нового режима

### Примечание по архитектуре

✅ **Сделано правильно:**
- Старые стили полностью консервируются
- Новая тема изолирована в отдельном файле
- Используются CSS-переменные
- Плавные переходы между темами
- Состояние сохраняется в localStorage

❌ **Что нельзя делать:**
- Не удалять или изменять существующие стили
- Не использовать !important без необходимости
- Не создавать новые скрипты, если можно обойтись CSS
- Не нарушать структуру DOM

---

## 📊 Характеристики Fallout-режима

| Параметр | Значение |
|----------|----------|
| Основной цвет | `#FFB642` (P1 Phosphor Amber) |
| Фон | `#050505` (Глубокий чёрный) |
| Дополнительный цвет | `#FFD700` (Яркий жёлтый) |
| Опасность | `#FF6B35` (Оранжевый) |
| Время переходов | 0.6s |
| Кривая анимации | cubic-bezier(0.25, 0.46, 0.45, 0.94) |

---

## 🎨 Визуальные эффекты

- **CRT сканлайны** - адаптированы под янтарный цвет
- **Виньетирование** - затемнение по краям экрана
- **Мерцание** - имитация старого монитора
- **Гловы** - янтарное свечение вокруг активных элементов
- **Hover-эффекты** - инверсия цвета при наведении

---

## 🔧 Troubleshooting

### Тема не переключается:
- Проверьте, загружается ли `fallout-theme.css`
- Откройте DevTools (F12) → Console и проверьте ошибки
- Убедитесь, что JavaScript не выключен

### Тема не сохраняется при перезагрузке:
- Проверьте, не в режиме ли приватного браузера
- localStorage может быть отключён - проверьте настройки браузера

### Кнопка не видна:
- На логине: она должна быть справа, слева от INFO-кнопки
- На терминале: наведите мышку на верхний правый угол панели управления

### Анимация "подвисает":
- Проверьте количество открытых вкладок
- Отключите другие расширения браузера
- Обновите браузер (обычно помогает)

---

**Версия:** 1.0  
**Дата:** 2026-02-02  
**Готово к использованию** ✓
